import Link from "next/link";
export default async function DocumentDetail({ params }: PageProps<"/documents/[id]">) { const { id } = await params; return <section className="space-y-6"><Link href="/documents" className="text-sm font-medium text-indigo-600">← All documents</Link><div className="rounded-xl border border-slate-200 bg-white p-7 shadow-sm"><p className="text-sm font-medium text-indigo-600">Document detail</p><h1 className="mt-2 text-3xl font-semibold">Document {id}</h1><p className="mt-3 text-sm leading-6 text-slate-600">This route is ready for document metadata, preview, access controls, and retrieval citations. Those will become live once file storage and the database are added.</p></div></section>; }
